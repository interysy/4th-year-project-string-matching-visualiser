import { AdditionalVariables } from "../../models/additional-variables.model";

/**
 * @description The additional variables needed for brute force algorithm to run.
 */
export class BruteForceAdditionalVariables extends AdditionalVariables {
    startingPoint : number;
}