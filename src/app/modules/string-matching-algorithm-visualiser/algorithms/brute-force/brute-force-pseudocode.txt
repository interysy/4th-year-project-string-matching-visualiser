bruteForce(text : string , pattern : string) : number {
    const textLength = text.length;</code>
    const patternLength = pattern.length;</code>
    let startingPoint = 0;</code>
    let textIndex = 0;</code>
    let patternIndex = 0;</code>

    while (startingPoint <= textLength - patternLength && patternIndex < patternLength) {
        if (text.charAt(textIndex) === pattern.charAt(patternIndex)) {
            textIndex++;
            patternIndex++;
        } else {
            patternIndex = 0;
            startingPoint += 1;
            textIndex = startingPoint;
        }
    }

    if (patternIndex === patternLength) {{ '{' }}
        return startingPoint;
    }
    return -1
}
